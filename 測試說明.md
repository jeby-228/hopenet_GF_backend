# 本專案的測試是如何寫的

## 快速回答

本專案使用 **兩種測試方法**：

### 1. **Hurl 集成測試** (現有的)

- 檔案: `test.hurl`
- 用途: 快速測試 API 端點
- 執行方式: `hurl --test test.hurl`

### 2. **Pytest 測試** (新增的範例)

- 位置: `src/backend/tests/`
- 包含:
  - **單元測試** (`tests/unit/`): 測試個別元件 (models, schemas)
  - **集成測試** (`tests/integration/`): 測試 API 端點與資料庫互動
- 執行方式: `cd src/backend && uv run pytest`

---

## 測試架構

```
src/backend/
├── tests/
│   ├── conftest.py              # pytest 配置與共用 fixtures
│   ├── unit/                    # 單元測試
│   │   ├── test_models.py       # 測試資料模型
│   │   └── test_schemas.py      # 測試 Pydantic schemas
│   └── integration/             # 集成測試
│       └── test_task_api.py     # 測試任務 API
```

---

## 測試範例說明

### 單元測試範例 (test_models.py)

```python
def test_task_creation_with_minimal_fields(self):
    """測試建立最簡任務"""
    task = Task(title='測試任務')
    
    assert task.title == '測試任務'
    assert task.is_deleted is False
    assert task.weight == 50  # 預設值
```

**特點:**
- 測試單一元件（Task 模型）
- 不需要資料庫
- 執行快速
- 驗證預設值、欄位驗證等

### 集成測試範例 (test_task_api.py)

```python
async def test_get_task_by_id(self, client: AsyncClient, test_task: Task):
    """測試取得任務"""
    response = await client.get(f'/api/v1/task/{test_task.id}')
    
    assert response.status_code == 200
    json_data = response.json()
    assert json_data['id'] == test_task.id
```

**特點:**
- 測試完整的 API 流程
- 使用測試資料庫
- 測試 HTTP 請求與回應
- 驗證 API 行為正確

---

## Fixtures 說明 (conftest.py)

Fixtures 是 pytest 的核心功能，用於準備測試所需的資源：

```python
@pytest.fixture
async def test_user(test_session: AsyncSession) -> User:
    """建立測試使用者"""
    user = User(email='test@example.com', ...)
    test_session.add(user)
    await test_session.commit()
    return user
```

**常用的 fixtures:**

- `test_session`: 提供測試資料庫連線
- `client`: 提供測試用的 HTTP 客戶端
- `test_user`: 建立測試使用者
- `test_task`: 建立測試任務
- `auth_headers`: 提供認證標頭

---

## 如何執行測試

### 1. 安裝依賴

```bash
cd src/backend
uv sync --dev
```

### 2. 執行測試

```bash
# 執行所有測試
uv run pytest

# 執行單元測試
uv run pytest tests/unit/

# 執行集成測試
uv run pytest tests/integration/

# 執行特定檔案
uv run pytest tests/unit/test_models.py

# 執行特定測試函數
uv run pytest tests/unit/test_models.py::TestTaskModel::test_task_creation_with_minimal_fields

# 顯示詳細輸出
uv run pytest -v

# 程式碼覆蓋率
uv run pytest --cov=app --cov-report=html
```

### 3. 執行 Hurl 測試

```bash
# 確保服務運行中
docker-compose up -d

# 執行測試
hurl --test test.hurl
```

---

## 測試策略

### 測試金字塔

```
       /\
      /  \     少量集成測試 (Integration Tests)
     /----\    測試 API 與資料庫互動
    /      \   
   /--------\  大量單元測試 (Unit Tests)
  /__________\ 測試個別元件邏輯
```

### 本專案的測試分工

1. **Hurl 測試** (`test.hurl`)
   - 快速驗證 API 端點
   - 適合手動測試與 CI/CD
   - 不需要寫 Python 程式碼

2. **單元測試** (`tests/unit/`)
   - 測試 models、schemas 的邏輯
   - 不依賴外部資源（資料庫、API）
   - 執行速度快
   - 容易維護

3. **集成測試** (`tests/integration/`)
   - 測試 API 端點完整流程
   - 使用測試資料庫
   - 驗證元件間的互動
   - 模擬真實使用情境

---

## 測試最佳實踐

### 1. 測試命名

```python
def test_<功能>_<情境>_<預期結果>():
    # 範例
    def test_task_creation_with_invalid_status_raises_error():
        ...
```

### 2. AAA 模式

```python
def test_example():
    # Arrange (準備)
    task = Task(title='測試')
    
    # Act (執行)
    result = task.title
    
    # Assert (驗證)
    assert result == '測試'
```

### 3. 使用 Fixtures

```python
def test_with_fixture(test_user):  # 自動注入 fixture
    assert test_user.email == 'test@example.com'
```

### 4. 測試邊界情況

- 空值
- 無效輸入
- 極端值
- 錯誤狀態

### 5. 保持測試獨立

每個測試應該能夠獨立執行，不依賴其他測試的結果。

---

## 常見測試場景

### 1. 測試資料驗證

```python
def test_invalid_email_raises_error():
    with pytest.raises(ValidationError):
        User(email='invalid-email')
```

### 2. 測試 API 認證

```python
async def test_unauthorized_access(client):
    response = await client.post('/api/v1/task', json={'title': '任務'})
    assert response.status_code == 401
```

### 3. 測試資料庫操作

```python
async def test_task_saved_to_database(test_session):
    task = Task(title='新任務')
    test_session.add(task)
    await test_session.commit()
    
    assert task.id is not None
```

### 4. 測試分頁

```python
async def test_pagination(client, test_tasks):
    response = await client.get('/api/v1/task?page=1&itemsPerPage=2')
    data = response.json()
    
    assert len(data['data']) <= 2
    assert data['page'] == 1
```

---

## 持續集成 (CI)

建議在 GitHub Actions 中自動執行測試：

```yaml
# .github/workflows/test.yml
- name: Run tests
  run: |
    cd src/backend
    uv run pytest --cov=app
```

---

## 總結

本專案的測試方法：

✅ **現有**: Hurl 集成測試 (`test.hurl`)  
✅ **新增**: Pytest 單元測試 + 集成測試  
✅ **配置**: `pyproject.toml` 中的 pytest 設定  
✅ **Fixtures**: `conftest.py` 中的共用測試資源  
✅ **文檔**: 詳細的 `TESTING.md` 說明

完整文檔請參閱:
- [TESTING.md](../TESTING.md) - 完整測試指南（中英文）
- [test.hurl](../test.hurl) - Hurl 測試範例
- [tests/](../src/backend/tests/) - Pytest 測試範例

---

## 下一步

1. **執行測試**: 試著執行 `uv run pytest` 看看測試如何運作
2. **閱讀範例**: 查看 `tests/unit/` 和 `tests/integration/` 中的測試範例
3. **撰寫測試**: 為新功能撰寫測試
4. **檢查覆蓋率**: 使用 `pytest --cov=app` 確保程式碼有足夠的測試覆蓋

有任何問題請參考 [TESTING.md](../TESTING.md) 的常見問題部分！
